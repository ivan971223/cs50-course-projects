#include <ctype.h>
#include <cs50.h>
#include <stdio.h>
#include <string.h>

int count_letters(string text)

int main(void)
{
    string text = get_string("Text:");
    printf("Text: %s\n", text);

}

int count_letters(string text)
{
    int len = strlen(text);
    for (int i=0; i<len; i++)
    {
        if(text[i]>=
    }
}